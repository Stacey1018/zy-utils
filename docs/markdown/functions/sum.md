[**zy-utils**](../README.md)

***

[zy-utils](../globals.md) / sum

# Function: sum()

> **sum**(`a`, `b`): `number`

Defined in: [sum.ts:9](https://github.com/Stacey1018/zy-utils/blob/63cfc6767b04c16e71b4602dd007514b3398c5cd/src/sum.ts#L9)

The function `sum` takes two numbers as input and returns their sum.

## Parameters

### a

`number`

The parameter `a` is a number.

### b

`number`

The parameter `b` in the `sum` function is a number that is being added to the
parameter `a` to calculate the sum.

## Returns

`number`

The sum of the two input numbers, `a` and `b`, is being returned.
